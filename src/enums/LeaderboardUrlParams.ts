export enum LeaderboardUrlParams {
  USER = "user",
  PLATFORM = "platform",
  VERSION = "version",
}

export enum Platform {
  CROSSPLAY = "crossplay",
  STEAM = "steam",
  PLAYSTATION = "psn",
  XBOX = "xbox",
}

export enum FilterField {
  USER,
  PLATFORM,
}
